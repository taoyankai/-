# 华中商贸配送中心项目：代码与业务功能解析

> 生成日期：2026-09-17  
> 阅读对象：首次接手项目的产品、开发、测试和运维人员  
> 结论依据：当前项目源码、数据库脚本和部署配置；历史文档仅用于辅助理解

---

## 1. 一句话理解这个项目

这是一个面向教职工的慰问品领取和配送信息采集系统。

它解决的不是“用户在线付款购物”，而是以下业务：甲方先提供有资格领取慰问品的教职工名单，教职工通过手机号完成身份核验，在限定套餐中选择一份，填写精确到门牌的收货地址和期望承运商；运营人员随后在后台管理名单、库存、订单、发货、物流状态、10 日发货 SLA 和数据导出审计。

核心业务链路如下：

```text
甲方导入教职工名单
        ↓
教职工手机号 + 验证码登录
        ↓
浏览套餐并选择一份
        ↓
填写配送地址、代收点授权、期望承运商
        ↓
系统校验资格、额度、地址和库存，生成待发货订单
        ↓
运营后台导出待发货单 / 录入运单号 / 批量发货
        ↓
人工推进物流状态，教职工查询轨迹
        ↓
签收完成
```

项目强调的是“名单制领取、配送履约与合规留痕”，而不是支付、购物车、退款或复杂商品交易。

---

## 2. 项目组成和技术架构

### 2.1 目录分工

| 目录或文件 | 实际作用 |
| --- | --- |
| `server/` | Java 17、Spring Boot 3.2.5 后端，负责全部业务规则与接口 |
| `miniprogram/` | 原生微信小程序，10 个页面，供教职工使用 |
| `miniapp/index.html` | 消费者 H5 备用入口，纯 HTML/CSS/JavaScript 单文件 |
| `admin/index.html` | 运营管理后台，纯 HTML/CSS/JavaScript 单文件 |
| `cloudfunctions/api/` | 微信云函数代理，把小程序请求转发到 Spring Boot 后端 |
| `db/init.sql` | MySQL 建表、演示名单、套餐、公告和演示订单 |
| `deploy/nginx/` | Nginx 静态资源与 API 反向代理配置 |
| `docker-compose.yml` | 本地 MySQL、Redis、API、Nginx 一键编排 |
| `deploy/server/` | 服务器部署编排和环境变量模板 |
| `docs/` | 各阶段设计文档；应以 v4.0 和当前源码为准 |

### 2.2 运行时架构

```text
微信小程序 ── wx.cloud.callFunction ── 腾讯云函数 api ──┐
                                                         │
消费者 H5 ───────────────────────────────────────────────┤
                                                         ▼
运营后台 ────────────────────────────────→ Nginx → Spring Boot API
                                                     │        │
                                                     ▼        ▼
                                                   MySQL    Redis
```

- MySQL 保存名单、套餐、订单、地址、物流轨迹、公告、导入批次、审计日志和 SLA 预警。
- Redis 保存登录 Token、短信验证码、验证码频控、登录失败锁定状态以及订单号日序列。
- Nginx 对外提供 H5、后台和 `/api/` 反向代理。
- 小程序正式环境使用云函数中转，避免 `wx.request` 对备案域名和 HTTPS 的限制。

### 2.3 主要技术

- 后端：Java 17、Spring Boot 3.2.5、MyBatis-Plus 3.5.5。
- 数据：MySQL 8.0、Redis 7.4。
- Excel：EasyExcel 3.3.4，用于名单和运单批量导入、数据导出。
- 密码：PBKDF2-HMAC-SHA256，加随机盐，后台 Token 存 Redis。
- 前端：消费者 H5 和运营后台均为原生 JavaScript 单页文件；微信端为原生小程序。
- 部署：Docker Compose + Nginx。

> 源码核验差异：交接说明把 `admin/` 描述成“Vue 3 + Element Plus + ECharts”，但当前文件没有引入这些依赖，实际是约 102 KB 的原生 JavaScript 单文件后台。后续维护应以源码为准。

---

## 3. 两类用户和权限

### 3.1 教职工用户（C 端）

教职工不是自由注册，而是必须预先存在于 `t_grantee` 名单表中。手机号是登录标识。

系统支持两种身份核验方式，由 `LOGIN_MODE` 控制：

- `sms`：手机号 + 短信验证码，是当前配置默认方式。
- `name`：手机号 + 姓名，仅适合演示，不应作为正式环境的安全方案。

登录成功后，后端生成随机 Token，写入 Redis，有效期 7 天。客户端把 Token 放到 `X-Token` 请求头中。

安全措施包括：

- 名单外手机号不发送验证码。
- 同一手机号 60 秒内不能重复发送。
- 每个手机号有每日短信次数上限，默认 10 次。
- 登录连续失败默认 5 次后锁定 10 分钟。
- 短信验证码登录成功后立即删除验证码，不能重复使用。
- 免登录的名单检查接口只返回“是否在名单”，不返回姓名、单位等个人信息。

主要实现：

- `server/src/main/java/com/hz/delivery/service/AuthService.java`
- `server/src/main/java/com/hz/delivery/controller/ClientAuthController.java`
- `server/src/main/java/com/hz/delivery/config/AuthInterceptor.java`

### 3.2 后台用户

系统首次启动且 `t_admin` 为空时，会自动创建三个账号：

| 账号 | 角色 | 当前代码中的权限 |
| --- | --- | --- |
| `admin` | 管理员 | 查询和写操作 |
| `operator` | 运营人员 | 查询和写操作 |
| `viewer` | 只读人员 | 只允许 GET 查询和导出，禁止写操作 |

初始密码由 `ADMIN_INIT_PASSWORD` 环境变量提供。后台 Token 有效期 12 小时。

当前权限模型比较粗：代码只对 `viewer` 做了写操作拦截，`admin` 与 `operator` 在接口权限上基本相同，并没有细粒度菜单或动作授权。

主要实现：

- `server/src/main/java/com/hz/delivery/config/DataInitializer.java`
- `server/src/main/java/com/hz/delivery/service/AdminAuthService.java`
- `server/src/main/java/com/hz/delivery/common/PasswordUtil.java`

---

## 4. C 端实现的业务功能

### 4.1 登录与资格核验

登录前端先读取 `/api/client/auth/mode`，决定显示验证码还是姓名输入框。短信模式的流程为：

1. 输入手机号。
2. 后端检查手机号是否在启用的领取名单中。
3. 通过 Mock、河北大学短信平台或预留的阿里云通道发送验证码。
4. 用户提交手机号和验证码。
5. 后端核验失败次数和锁定状态，成功后签发 Token。

短信通道实现：

- `MockSmsSender`：本地演示，不实际发短信。
- `HbuSmsSender`：河北大学统一短信平台，当前正式通道。
- `AliyunSmsSender`：仅有接口骨架，尚不能视为完整可用的阿里云短信集成。

### 4.2 首页和公告

首页读取个人领取信息和已发布公告，展示：

- 姓名、单位、部门。
- 可领取份数和已领取份数。
- 公告、配送说明、偏远地区时效说明。
- 套餐选择、订单、物流查询、个人中心入口。

### 4.3 套餐浏览

教职工可以查看当前上架的套餐、库存和套餐内的商品清单。C 端没有修改套餐的能力。

套餐由两张表组成：

- `t_package`：套餐主信息、价格、库存、预警值和上下架状态。
- `t_package_item`：套餐内的商品名称、规格、数量和单位。

### 4.4 提交配送信息，也就是“下单”

下单是项目最核心的事务。后端 `OrderService.create()` 按以下顺序执行：

1. 验证登录人仍在名单中且状态正常。
2. 校验收件人、手机号、省市区和详细地址。
3. 阻止同一人对同一套餐重复提交待发货订单。
4. 校验领取额度，默认每人只能领取 1 份。
5. 检查套餐是否存在且已上架。
6. 使用带 `stock >= qty` 条件的 SQL 原子扣减库存，防止并发超卖。
7. 使用带 `used < quota` 条件的 SQL 原子占用领取额度，防止并发多领。
8. 把套餐商品内容序列化为 JSON 快照写入订单，确保套餐以后修改也不会改变历史订单。
9. 保存收货信息、代收点授权、期望承运商和备注。
10. 设置订单为“待发货”，计算提交时间后 10 天的 SLA 截止时间。
11. 写入第一条物流轨迹。

库存扣减、额度占用和订单写入处在同一数据库事务中；中途失败会一起回滚。

### 4.5 地址约束

由于业务要求“上楼入户、送达门牌”，服务端不是只判断地址非空，而是要求：

- 收件人不为空。
- 手机号是 11 位中国大陆手机号格式。
- 省、市、区完整。
- 详细地址不少于 8 个字符。
- 详细地址包含数字。
- 详细地址包含“室、栋、幢、号楼、单元、号、小区、公寓”等门牌类关键词之一。

“是否允许放代收点”默认是 `false`，用户必须主动授权。这个值会保存到地址和订单中，并出现在发货清单里。

主要实现：`server/src/main/java/com/hz/delivery/service/AddressValidator.java`。

### 4.6 地址簿和订单地址修改

用户可以保存、查看和删除常用地址。下单时也可以选择保存地址。

订单地址修改有两个硬限制：

- 仅待发货状态可以修改；一旦发货就锁定。
- 用户最多自助修改 1 次。

修改动作会写入物流轨迹，便于运营在发货前核对和事后追溯。

### 4.7 订单取消

只有待发货订单可以由用户取消。取消成功后：

- 订单状态改为 `50`（已取消）。
- 套餐库存回补。
- 教职工已用额度减 1。
- 记录取消时间和物流轨迹。

已发货订单不能自助取消，需要联系客服处理。

### 4.8 订单和物流查询

用户可以：

- 查看自己的全部订单或按状态筛选。
- 查看订单详情和商品快照。
- 查看物流轨迹。
- 输入运单号查询，但后端会验证该运单是否属于当前登录人，防止越权查询他人订单。

订单状态为：

| 状态值 | 含义 |
| --- | --- |
| `0` | 待发货 |
| `10` | 已发货 |
| `20` | 运输中 |
| `30` | 派送中 |
| `40` | 已签收 |
| `50` | 已取消 |
| `60` | 配送异常 |
| `70` | 已退回 |

### 4.9 小程序页面对应关系

| 页面 | 功能 |
| --- | --- |
| `pages/login` | 身份核验、获取验证码和登录 |
| `pages/index` | 首页、领取状态和公告 |
| `pages/packages` | 套餐列表 |
| `pages/package` | 套餐详情 |
| `pages/confirm` | 新订单确认或待发货订单地址修改 |
| `pages/result` | 提交结果 |
| `pages/orders` | 我的订单列表 |
| `pages/order` | 订单详情、取消、修改地址 |
| `pages/track` | 运单查询与物流轨迹 |
| `pages/mine` | 个人资料、说明和退出登录 |

---

## 5. 运营后台实现的业务功能

后台是一个单文件管理页面，菜单包括：数据看板、教职工名单、套餐管理、订单汇总、发货管理、物流跟踪、SLA 监控、报表中心、公告与说明、操作日志。

### 5.1 数据看板与报表

看板统计：

- 订单总数和各状态数量。
- 签收率。
- 名单总人数、已领取人数、未领取人数和领取率。
- 待发货、已超时、24 小时内到期、72 小时内到期数量。
- 按单位统计订单。
- 按套餐统计选择量。
- 近 14 天趋势。
- 库存低于预警阈值的套餐。

报表中心提供单位、套餐和近 30 天趋势数据。

主要实现：`server/src/main/java/com/hz/delivery/service/StatsService.java`。

### 5.2 教职工名单管理

后台可以：

- 分页、关键词、单位和领取状态筛选。
- 下载 Excel 导入模板。
- 批量导入名单。
- 选择是否覆盖已存在手机号的数据。
- 新增或更新单个名单人员。
- 删除未领取人员；已领取人员禁止删除，只能停用。
- 重置领取额度，并处理其待发货订单。
- 按筛选条件导出名单及配送相关数据。
- 查看最近 100 个导入批次。

Excel 导入会校验姓名、手机号格式、文件内重复手机号和数据库已存在手机号，并记录每批导入的成功数、失败数和失败原因。

### 5.3 套餐与库存管理

后台可以新建、修改、上下架和删除套餐，维护套餐明细及库存预警值。

修改已有套餐时必须填写变更原因。订单创建时保存套餐快照，所以后续套餐变化不会影响历史订单。

需要注意：当前数据库只在套餐主表保存“最近一次变更原因”和更新时间，没有独立的套餐版本历史表。因此它能保住历史订单内容，但并不能完整还原套餐的每一次历史修改。

### 5.4 订单汇总

运营可以按以下条件查询订单：

- 订单状态。
- 单位。
- 套餐。
- 关键词，如订单号、姓名、工号、手机号、地址、运单号。
- SLA 等级。

也可以查看订单详情、收货地址、套餐快照、代收点授权、期望承运商、实际承运商、运单号和完整轨迹，并按筛选条件导出 Excel。

### 5.5 发货管理

后台支持两种发货方式：

- 勾选一个或多个待发货订单，选择承运商，手动录入运单号或由系统模拟生成。
- 下载运单模板，再批量导入订单号、承运商和运单号。

发货 SQL 带 `status = 0` 条件，避免并发重复发货。成功后订单进入“已发货”，写入实际承运商、运单号和发货时间，同时新增物流轨迹。

系统还提供“撤回发货”用于纠正录入错误，但只允许把“已发货”退回“待发货”。

### 5.6 物流跟踪

运营可以手动把已发货订单推进到运输中、派送中、已签收、配送异常或已退回，并填写轨迹说明。

当前源码没有真正接入快递公司的轨迹查询或回调 API；物流状态主要依赖人工操作。因此“全程物流跟踪”目前是业务数据管理能力，不是自动同步能力。

### 5.7 SLA 监控

订单提交时，系统按 `SLA_DAYS` 计算发货截止时间，默认 10 天。

待发货订单分为：

- 等级 1：72 小时内到期。
- 等级 2：24 小时内到期。
- 等级 3：已经超期。

定时任务默认每天 09:30 扫描：

- 把各等级订单数和最多 30 个订单号写入 `t_sla_alert`。
- 如果有超期或 24 小时内到期订单，并配置了 `SLA_ALERT_PHONE`，则向库房管理员发送短信。
- 同一天、同一等级重复扫描时更新原记录，不重复插入。

运营也可以手动触发扫描。待发货订单若超时，可以登记原因、备注和预计处理时间，信息会写入订单并增加一条轨迹。

### 5.8 公告与说明

后台维护三类内容：

- 首页公告。
- 配送说明。
- 偏远地区说明或时效说明。

只有启用状态的内容会展示给 C 端用户。

### 5.9 操作审计

`t_operation_log` 可以记录操作人账号、姓名、角色、模块、动作、目标、详细条件、影响条数、IP、User-Agent、成功或失败状态。

当前源码明确接入审计的主要操作是：

- 名单导入。
- 名单导出。
- 订单明细导出。
- 待发货清单导出。
- 手动 SLA 扫描。

套餐修改原因另存在套餐表；普通新增、修改、删除、发货和物流推进等动作并没有全部写入统一审计表。若项目需要“所有后台写操作完整留痕”，这里仍需扩展。

---

## 6. 核心数据模型

| 数据表 | 作用 | 关键关系 |
| --- | --- | --- |
| `t_admin` | 后台账号 | Redis 保存其登录 Token |
| `t_grantee` | 教职工领取资格名单 | 一人可有多个地址和订单；手机号作为身份标识 |
| `t_package` | 套餐主表 | 一对多关联 `t_package_item` |
| `t_package_item` | 套餐商品明细 | 归属于套餐 |
| `t_order` | 配送订单 | 关联教职工和套餐，并冗余人员、套餐、地址信息 |
| `t_order_track` | 物流轨迹 | 多条轨迹归属于一个订单 |
| `t_address` | 教职工地址簿 | 多条地址归属于一个教职工 |
| `t_notice` | 公告与配送说明 | 按类型和状态展示 |
| `t_import_batch` | Excel 导入批次 | 保存名单或运单导入结果 |
| `t_carrier` | 承运商字典 | 订单保存期望和实际承运商 |
| `t_operation_log` | 后台操作审计 | 记录敏感导入、导出等操作 |
| `t_sla_alert` | 发货预警 | 按日期和预警等级唯一 |

订单表是系统的业务中心。它故意冗余了姓名、单位、套餐名称、套餐 JSON 快照和完整收货地址，即使名单或套餐以后修改，历史订单仍能保留当时的业务事实。

---

## 7. API 模块速查

### 7.1 C 端接口

| 路径组 | 功能 |
| --- | --- |
| `/api/client/auth/mode` | 查询当前登录方式 |
| `/api/client/auth/check` | 检查手机号是否在名单内 |
| `/api/client/auth/send-code` | 发送短信验证码 |
| `/api/client/auth/login`、`logout` | 登录和退出 |
| `/api/client/profile` | 当前教职工信息和领取额度 |
| `/api/client/packages` | 套餐列表和详情 |
| `/api/client/notices` | 公告与配送说明 |
| `/api/client/carriers` | 可选承运商 |
| `/api/client/addresses` | 地址簿增删查 |
| `/api/client/addresses/validate` | 预校验配送地址 |
| `/api/client/orders` | 创建和查询本人订单 |
| `/api/client/orders/{id}/cancel` | 取消待发货订单 |
| `/api/client/orders/{id}/address` | 修改一次待发货订单地址 |
| `/api/client/orders/track` | 按本人运单号查物流 |

### 7.2 后台接口

| 路径组 | 功能 |
| --- | --- |
| `/api/admin/auth/*` | 后台登录、资料和退出 |
| `/api/admin/dashboard`、`reports` | 看板与报表 |
| `/api/admin/grantees/*` | 名单、Excel 导入导出、额度重置和批次 |
| `/api/admin/packages/*` | 套餐、库存和上下架 |
| `/api/admin/orders/*` | 订单、轨迹、发货、运单导入、导出和超时说明 |
| `/api/admin/notices/*` | 公告管理 |
| `/api/admin/sla/*` | SLA 预警记录和手动扫描 |
| `/api/admin/sms/*` | 短信通道状态与受控测试 |
| `/api/admin/operation-logs` | 审计日志查询 |

统一响应格式为：

```json
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```

业务错误通常仍以 HTTP 200 返回，通过 `code` 判断成功与失败。这是现有前端解包逻辑的基础，修改 API 风格时要同步调整前端。

---

## 8. 微信小程序为什么多了一层云函数

小程序请求层支持两条通道：

- `useCloud=false`：使用 `wx.request` 直连后端，只适合本地开发调试。
- `useCloud=true`：调用名为 `api` 的云函数，再由云函数转发到后端，是当前正式配置。

页面调用 `/client/orders` 时，云函数会拼成后端的 `/api/client/orders`。Token 由小程序传给云函数，再转为后端的 `X-Token` 请求头。

云函数只使用 Node.js 内置 `http`/`https`，`wx-server-sdk` 是可选依赖。后端地址优先读取云函数环境变量 `BACKEND_BASE_URL`，否则使用源码中的公网 IP 默认值。

需要留意：后端地址目前在云函数和小程序调试配置中都存在硬编码公网 IP。服务器迁移时，这两个位置和云函数环境变量都应检查。

---

## 9. 本地部署是如何工作的

运行：

```bash
docker-compose up -d --build
```

会启动四个容器：

| 容器 | 作用 | 本地端口 |
| --- | --- | --- |
| `hz-mysql` | MySQL 8.0；首次空数据卷时执行 `db/init.sql` | 3306，默认对宿主机开放 |
| `hz-redis` | Token、验证码、频控、锁定和订单号序列 | 6379，默认对宿主机开放 |
| `hz-api` | Spring Boot 后端 | 8080 |
| `hz-nginx` | H5、后台和 API 统一入口 | 80 |

访问入口：

- 消费者 H5：`http://localhost/`
- 运营后台：`http://localhost/admin/`
- API：`http://localhost/api/`
- Spring Boot 健康检查：`http://localhost/actuator/health`

注意：Nginx 的 `/healthz` 当前直接返回静态文本 `ok`，只能说明 Nginx 正常，不能证明 Spring Boot、MySQL 和 Redis 都正常。需要判断完整后端状态时应使用 `/actuator/health` 或实际业务接口。

---

## 10. 源码中已经落实的关键业务保护

1. 名单制身份：不在名单内不能登录和领取。
2. 验证码防滥用：60 秒频控、每日上限、失败回退额度。
3. 登录防枚举：连续失败锁定，名单预检不泄露个人资料。
4. 防超领：额度通过条件更新原子占用。
5. 防超卖：库存通过条件更新原子扣减。
6. 防重复提交：同人同套餐已有待发货订单时拒绝再次创建。
7. 历史订单稳定：订单保存套餐内容快照和人员、地址冗余信息。
8. 地址履约：服务端强制校验到数字门牌/楼栋信息。
9. 代收点授权：默认不允许，必须由用户明确开启。
10. 发货防重复：发货更新要求订单仍处于待发货状态。
11. 地址防错发：发货后锁定，发货前最多自助改一次。
12. SLA 主动预警：每日定时扫描，而不只依赖运营主动看后台。
13. 敏感导出留痕：名单和订单导出记录操作条件、条数、IP 和浏览器信息。
14. 只读后台角色：`viewer` 不能执行写操作。

---

## 11. 当前实现的边界和接手时应注意的问题

### 11.1 这不是完整电商系统

项目没有购物车、在线支付、退款、发票、促销、商品自由组合等模块。“订单”本质上是一次慰问品领取和配送资料提交记录。

### 11.2 物流状态不是自动同步

承运商表保存查询链接模板，但当前没有快递 API 回调或自动拉取轨迹。物流状态和轨迹主要由后台人工推进。

### 11.3 权限控制较粗

`viewer` 是只读角色，但 `admin` 与 `operator` 没有进一步区分。若后续有多人协作、数据分级或审批要求，需要补充细粒度权限模型。

### 11.4 套餐变更不是完整版本审计

订单快照能保证历史订单不变，但套餐表只留最近一次变更原因。如果要求证明套餐的每次修改，需要新增套餐版本表或统一写入操作日志。

### 11.5 统一审计覆盖不完整

目前重点覆盖导入、导出和 SLA 扫描，并非所有后台写操作。发货、撤回发货、物流推进、套餐删除、公告修改等操作建议补齐审计。

### 11.6 前端单文件便于部署，但不利于长期维护

后台和 H5 都把 HTML、样式和业务脚本放在一个文件里。当前体量已分别约 102 KB 和 83 KB，继续扩展会使模块复用、自动化测试、构建优化和多人协作越来越困难。

### 11.7 数据库约束可进一步加强

应用层会阻止重复手机号和重复订单号，但数据库脚本中手机号、订单号和运单号使用的是普通索引，不是唯一索引。高并发或绕过应用直接写库时仍可能出现重复数据。正式大规模使用前可评估增加唯一约束。

### 11.8 初始数据是演示数据

`db/init.sql` 包含演示名单、套餐、公告和订单。正式上线前必须用甲方真实名单替换，并防止真实短信发到演示假号码。

### 11.9 两套健康检查含义不同

- `/healthz`：Nginx 静态返回，说明入口活着。
- `/actuator/health`：Spring Boot 实际健康状态，更适合部署验收。

---

## 12. 建议的代码阅读顺序

如果只有 1 小时，按以下顺序阅读：

1. `db/init.sql`：先理解业务数据和状态。
2. `server/src/main/java/com/hz/delivery/service/AuthService.java`：理解谁能登录。
3. `server/src/main/java/com/hz/delivery/service/OrderService.java`：理解最核心的下单事务。
4. `server/src/main/java/com/hz/delivery/service/ShipmentService.java`：理解发货和物流。
5. `server/src/main/java/com/hz/delivery/service/SlaAlertService.java`：理解 10 日发货约束。
6. `server/src/main/java/com/hz/delivery/controller/ClientController.java`：看 C 端能力边界。
7. `server/src/main/java/com/hz/delivery/controller/AdminOrderController.java`：看运营能力边界。
8. `miniprogram/utils/api.js` 和 `miniprogram/pages/`：理解小程序调用流程。
9. `admin/index.html`：从菜单和 API 调用理解后台交互。
10. `docker-compose.yml`、`deploy/nginx/nginx.conf`、`cloudfunctions/api/index.js`：理解部署和网络链路。

---

## 13. 接手后的推荐验证流程

1. 复制 `.env.example` 为 `.env`，确认本地使用 Mock 短信通道。
2. 启动 Docker Compose，确认 MySQL、Redis、API、Nginx 都正常。
3. 用未领取的演示账号登录，走完“选套餐 → 填地址 → 提交订单”。
4. 在后台确认库存减少、用户额度占用、订单进入待发货池。
5. 尝试重复提交，确认会被额度或重复订单规则拦截。
6. 修改一次待发货地址，再次修改应被拒绝。
7. 在后台发货，确认用户端出现运单和轨迹，且地址不能再修改、订单不能再取消。
8. 手动推进运输中、派送中、已签收，确认时间线正确。
9. 导出名单和订单，确认操作日志中有对应记录。
10. 手动执行 SLA 扫描，确认预警记录生成。
11. 最后运行项目现有回归测试，确认修改前基线稳定。

---

## 14. 最终结论

当前项目已经具备一套可运行的“名单制慰问品领取与配送履约”闭环：资格核验、套餐选择、地址采集、库存与额度控制、订单、发货、物流轨迹、SLA、报表、Excel 导入导出和部分审计均已实现。

它最成熟的部分是下单事务中的业务保护：名单、额度、库存、地址、套餐快照和订单状态之间的约束比较完整。它目前最明显的边界是物流依赖人工推进、权限较粗、统一审计覆盖不全，以及两个 Web 前端采用大型单文件结构。后续开发应优先保持现有业务约束，再逐步补自动物流、完整审计和前端模块化。
