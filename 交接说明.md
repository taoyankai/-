# 华中商贸配送中心 · 项目交接说明

> 交接日期：2026-09-17
> 对应开发文档：`docs/华中商贸配送中心-开发文档-v4.0.md`
> 打包范围：全部源码 + 配置模板 + 部署脚本 + 回归测试 + 项目知识记录

---

## 一、这个项目是什么

**教职工慰问品线上选购与配送信息采集平台。**

依据招标文件 2.1.2「供货期限及地点」条款建设，供货与配送的履约方是**华中商贸配送中心**。
**它不是**通用物流平台或聚合配送平台——那是一期（v1.0/v2.0）的方向，已废弃，
文档留在 `docs/archive/` 与 `docs/聚合配送平台开发文档-v2.0.md`，仅供追溯，**不要照着开发**。

### 不可违背的业务约束（后续改动务必守住）

| 约束 | 落地方式 |
| --- | --- |
| 收到配送信息起 **10 日内发货** | SLA 倒计时引擎，T+7/8/9/10 四道预警，超时须登记原因 |
| **上楼入户、送达门牌** | 地址强制校验到门牌；配送单固定带「上楼入户」标记 |
| **未经允许不得放代收点** | C 端授权开关**默认关闭**，结果写入配送单同步承运商 |
| **不允许私自更改方案内容** | 套餐 C 端只读；后台变更强制填原因并留痕；订单存套餐快照 |
| 全国包邮 | 全站标识；偏远地区单独公示时效 |
| 货品充足 | 库存管理与超卖拦截 |
| **导出个人信息必须留痕** | `t_operation_log` 记录操作人/角色/IP/UA/模块/动作/筛选/条数；**审计写失败不得影响导出主流程** |

---

## 二、交接包里有什么、没有什么

### 有

| 目录 | 内容 |
| --- | --- |
| `server/` | Java 后端（Spring Boot 3.2 + MyBatis-Plus），含 `Dockerfile` / `pom.xml` |
| `admin/` | 运营后台（单文件 `index.html`，Vue 3 + Element Plus + ECharts） |
| `miniapp/` | 消费者端 H5（单文件 `index.html`） |
| `miniprogram/` | 微信小程序端（**10 个原生页面**，非 web-view 壳） |
| `cloudfunctions/api/` | 云函数转发入口（零第三方依赖，只用 Node 内置模块） |
| `db/init.sql` | 建表 + 种子数据（含测试名单、套餐、公告） |
| `deploy/` | Nginx 配置、服务器编排 `deploy/server/docker-compose.yml`、线上 `.env` 模板 |
| `docs/` | 开发文档 v4.0（现行）/ v3.0 / v2.0 / archive |
| `docker-compose.yml` | 本地开发编排（MySQL + Redis + API + Nginx） |
| `.env.example` | 环境变量模板（已脱敏，可直接复制为 `.env`） |
| `project.config.json` / `project.private.config.json` | 小程序工程配置（AppID、miniprogramRoot、cloudfunctionRoot） |
| `.workbuddy/tools/` | **回归测试与部署工具**（见第七节） |
| `.workbuddy/memory/` | 项目知识记录（踩坑与结论，**建议先读 `MEMORY.md`）** |

### 没有（有意排除）

| 排除项 | 原因 |
| --- | --- |
| 项目根 `.env` | **含真实密钥**，请按第五节单独交接 |
| `.workbuddy/sms-api-doc.txt` | **含短信平台真实密钥（明文）**，不外发 |
| `.workbuddy/shots/` | 开发期截图，3.2MB，无交接价值 |
| `.workbuddy/tmp/`、`tmp_dom.html` | 一次性临时文件 |
| `admin.bak.html` 等 `*.bak.*` | 历史备份，避免误用 |

---

## 三、本地跑起来（约 10 分钟）

### 前置

Docker Desktop（含 compose）、JDK 17、Node 18+、Python 3.10+、微信开发者工具。

### 步骤

```bash
# 1. 准备环境变量
cp .env.example .env        # 按需修改，至少改 MYSQL_ROOT_PASSWORD / ADMIN_INIT_PASSWORD

# 2. 启动（MySQL + Redis + API + Nginx）
docker-compose up -d --build

# 3. 等 API healthy（首次构建需几分钟）
docker-compose ps

# 4. 自检
curl http://localhost:8080/healthz          # 期望 {"status":"UP", ...}
curl http://localhost/api/client/auth/mode  # 期望 {"code":0,...,"mode":"name"}
```

访问入口：

| 入口 | 地址 |
| --- | --- |
| 消费者端 H5 | http://localhost/ |
| 运营后台 | http://localhost/admin/ |
| 接口 | http://localhost/api/ |

后台账号：`admin` / `operator` / `viewer`，初始密码＝`.env` 里的 `ADMIN_INIT_PASSWORD`。
**后台账号由启动时的 `DataInitializer` 创建，且仅在 `t_admin` 表为空时执行**——
所以复位数据后**必须重建 api 容器**，否则后台登录必然失败（见第八节坑 4）。

### C 端测试账号

**⚠️ 当前登录方式为 `LOGIN_MODE=sms`（手机号 + 短信验证码），姓名核验通道已关闭。**

下表账号中，**手机号都是演示用的假号码，收不到短信，因此已无法登录**；
只有最后一行张冲的 `15369865664` 是真实号码（项目负责人本人的号），可用于验证短信登录。
**正式使用前必须把名单整表换成甲方提供的真实手机号**，否则教职工无法登录。

| 手机号 | 姓名 | 状态 |
| --- | --- | --- |
| `13900139003` | 刘嘉怡 | 未领取（可走完整领取流程） |
| `13800138000` | 张明远 | 已领取（可看订单与物流轨迹） |
| `15369865664` | 张冲 | 未领取（工号 10000，河北大学 · 后勤处） |

---

## 四、线上环境现状

### C 端登录方式：短信验证码（2026-09-17 切换）

线上已从「手机号 + 姓名」切换为 **`LOGIN_MODE=sms`**（手机号 + 短信验证码）：

| 配置项 | 当前值 | 说明 |
| --- | --- | --- |
| `LOGIN_MODE` | `sms` | 姓名核验通道已关闭，再传姓名登录返回 1003 |
| `SMS_PROVIDER` | `hbu` | 河北大学统一短信平台，真实下发 |
| `SMS_HBU_KEY` | 已配置 | ⚠️ 原先是**空值**，会导致全员发不出验证码、无法登录，已补齐 |
| `MOCK_SMS_CODE` | 空 | 正式环境不留固定验证码（真实通道下本就会忽略，清空是第二道保险） |

已实测通过：真实下发 → 从 Redis 取码 → 登录成功 → 同码重放返回 1003（验证码一次性）。
自检工具：`python .workbuddy/tools/probe-sms-channel.py`（只验通道能否发出）；
`python .workbuddy/tools/test-online-sms-login.py`（**会真发一条短信**，跑线上全链路）。

> ⚠️ **线上名单里的手机号都是演示假号码**（见第三节），切到短信后**只有张冲的
> `15369865664` 能登录**。正式使用前必须整表换成甲方真实手机号。
> 另外假号码若被触发发送，**会真发短信到陌生人手机**（费用 + 骚扰），换名单前不宜公开入口。

### ⚠️ 有两项加固「已写进源码、但尚未部署到线上」

| 加固 | 内容 |
| --- | --- |
| 验证码每日上限 | 新增 `SMS_DAILY_LIMIT`（默认 10）。60 秒频控挡不住「拿着名单逐个号码轰炸」，故加每日总量闸门；发送失败会退还当日额度 |
| 名单探测接口收敛 | `GET /api/client/auth/check` 原先回传脱敏姓名 / 单位 / 部门，等同于把花名册开放给任何知道手机号的人；现只回「是否在名单」 |

原因：本机 Docker 配置了一个**不可用的手动代理**（`http://127.0.0.1:7890`），
导致构建容器外网请求全被拦截（443 TLS 握手失败），镜像无法重新构建。
**代码、配置、回归套件均已改好并随包交付，只差一次构建部署。**

部署到线上（在任一能正常构建的机器上）：

```bash
docker compose build api
docker save hz-delivery-server:1.0.0 | gzip > api.tar.gz
# 把 api.tar.gz 传到服务器后，在 /opt/hz-delivery/deploy/server 下：
docker load < api.tar.gz
docker compose up -d --force-recreate api     # 环境变量只有重建才生效
```

### 服务器

| 项 | 值 |
| --- | --- |
| 内网 IP | `10.191.19.25`（Ubuntu 20.04 / 4 核 8G） |
| 公网 IP | `221.192.236.175`（**已映射 80 / 443**；**22 端口未映射**） |
| 部署目录 | `/opt/hz-delivery` |
| 进入方式 | **SSH 仅校园网内可达** → 校外必须先连学校 VPN，否则内网与公网 IP 的 22 端口都超时 |

### 对外入口

| 入口 | 地址 |
| --- | --- |
| 消费者端 H5 | http://221.192.236.175/ |
| 运营后台 | http://221.192.236.175/admin/ |
| 接口健康检查 | http://221.192.236.175/healthz |

**只对外暴露 80**：MySQL / Redis / API 全在内部 `hz-net` 网络里，不对外映射端口。
线上编排用的是 `deploy/server/docker-compose.yml`（与开发机根目录那份的区别就在这里）。

### 部署方式（服务器不需要 JDK / Maven / 外网依赖）

```
开发机：docker compose build api  →  docker save | gzip
   ↓ SFTP
服务器：docker load  →  docker compose up -d
```

现成脚本：`.workbuddy/tools/ssh_deploy.py`（paramiko，凭据**只从环境变量读、不落盘**：
`HZ_SERVER_HOST` / `HZ_SERVER_PORT` / `HZ_SERVER_USER` / `HZ_SERVER_PASS`，
支持 `--cmd` / `--script` / `--put` / `--get` / `--check`）。

> ⚠️ 该服务器 **Docker Hub 直连不通**，必须在 `/etc/docker/daemon.json` 配
> `registry-mirrors: https://docker.m.daocloud.io`；apt 直连官方源仅约 162 KB/s，
> 换阿里云源快约 10 倍。

---

## 五、凭据交接清单 ★ 必须单独通过安全渠道交接，不要放进本压缩包

以下内容**均未包含在本包内**，请由交接方另行提供：

| # | 凭据 | 位置 / 来源 | 用途 |
| --- | --- | --- | --- |
| 1 | 项目根 `.env` 的实际取值 | 原开发机项目根目录 | 本地开发（含数据库密码、短信密钥、后台初始密码） |
| 2 | **服务器 SSH** | `root@10.191.19.25:22` 密码 | 部署与线上数据维护（需 VPN） |
| 3 | 线上 `/opt/hz-delivery/deploy/server/.env` | 服务器上，权限 600 | **线上真实配置**（与开发机 `.env` 互相独立） |
| 4 | 线上初始凭据文件 | 服务器 `/root/hz-initial-credentials.txt`（600） | 线上后台 / 数据库初始密码 |
| 5 | **短信平台密钥** | `.env` 的 `SMS_HBU_KEY` | 河北大学统一短信平台真实发送 |
| 6 | 微信小程序管理员权限 | mp 后台把接手人加为**开发者/体验成员** | 出码、上传、发布 |
| 7 | 云开发环境 | `cloud1-d2gar70gv5b552827` | 云函数通道 |

**已知不一致（务必注意）**：
- **线上后台的 admin 密码 ≠ 开发机 `.env` 里的 `ADMIN_INIT_PASSWORD`**
  （实测开发机那个密码在线上登录失败），以服务器 `/root/hz-initial-credentials.txt` 为准；
- 线上后台 UI **没有「编辑教职工」功能**（只有详情 / 重置额度 / 覆盖式导入）。
  改线上名单两条路：SSH 进服务器改库，或用后台接口 `POST /api/admin/grantees`
  （body 带 `id` 即走更新分支）。

---

## 六、微信小程序端

| 项 | 值 |
| --- | --- |
| AppID | `wx67f6685a1a7bc5f3` |
| 云开发环境 | `cloud1-d2gar70gv5b552827` |
| 云函数 | `api`（统一转发，零第三方依赖） |
| 主体 | **个人主体**（这是很多限制的根源） |
| 当前状态 | 体验版 `1.0.0` 已上传；**尚未备案、未提交审核** |

### 请求通道（改一个开关）

`miniprogram/config.js` 的 `useCloud`：

- `false` → `wx.request` 直连后端（**仅本地联调**，体验版/正式版里 IP + http 会被微信拒绝）
- `true` → `wx.cloud.callFunction('api')` → 云函数 → HTTP → 现有 Spring Boot 后端（**正式环境，免备案**）

**免备案原理**：`wx.request` 受「服务器域名」白名单约束（须已备案域名 + HTTPS），
而云函数运行在腾讯云侧、出网不受该白名单限制 → 后端只要有公网地址（IP + HTTP 亦可）即可。

> 小程序自身的 **ICP 备案仍是必须的**（个人主体也要），但那是「小程序备案」，
> **不需要域名、不需要服务器接入信息**（工信部核验单明示这两项小程序免填）。
> 域名备案只在选「`wx.request` 直连」时才必需。

### 云函数配置边界（踩过坑）

- `cloudfunctions/api/config.json` **只能声明 `permissions.openapi` / `triggers`，且不能有注释**；
  **环境变量与超时属「环境配置」，只能在云开发控制台配**（写进 config.json 不生效也不报错）；
  环境变量只认 `BACKEND_BASE_URL`（后端**根地址，不带 `/api`**）。
- ⚠️ **云函数默认超时只有 3 秒，务必在控制台改成 20 秒**。
- ⚠️ **`wx-server-sdk` 必须保持 try/catch 可选加载**：硬 require 会在「云端没装依赖」时
  直接以 `Cannot find module` 执行失败，而本函数并不使用它的任何能力。

### 个人主体的硬限制（决定"别人能不能看到"）

| 方式 | 谁能看到 | 人数上限 |
| --- | --- | --- |
| 预览码 | 仅管理员 / 项目成员 | 项目成员上限 |
| 体验版 | 仅你添加的「体验成员」 | **个人主体仅 15 人** |
| **正式发布** | **所有微信用户**（可搜、可扫码、可分享） | 无上限 |

**要让全体教职工用，必须走正式发布**，前置三关：① 小程序备案（管局审核 1–20 个工作日）
② 提交审核（选服务类目）③ 审核通过后发布。

---

## 七、回归测试（改动后必跑）

```bash
# 一键全跑：复位数据 → 清 Redis → 按 .env 重建 api 容器并等 healthy → 跑 10 个套件 → 汇总
python .workbuddy/tools/run-regression.py

# 也可只跑匹配关键字的套件
python .workbuddy/tools/run-regression.py carrier
```

当前基线：**428 项断言全绿**。另有三个独立套件：

| 脚本 | 项数 | 作用 |
| --- | --- | --- |
| `test-miniprogram-native.py` | 28 | 小程序静态校验（页面完整性、事件处理函数、样式类、真机白屏防线） |
| `test-cloudfunction-contract.py` | 36 | 云函数契约（加载真实 `index.js` 打真实后端） |
| `verify-cloudfunction.js` | 9 | 小程序模拟器端到端（需开发者工具开自动化） |

> `test-sms-live.py` 需要设 `HZ_SMS_PHONE`，否则跳过——**它会真发短信**。

---

## 八、已知坑（接手人必读，都是踩过的）

1. **`.workbuddy/tools/` 必须保持目录层级**——脚本用 `Path(__file__).parents[2]` 定位项目根，
   挪到别处（如 `tools/`）会算错根目录而全部失效。
2. **`docker compose`（CLI 插件）在 Windows 上会突然不可用**（报 `unknown command: docker compose`），
   改用独立二进制 **`docker-compose`**。`run-regression.py` 已自动探测。
3. **改 `.env` 后必须 `--force-recreate api`**，`docker restart` 不会重读 `.env`。
4. **`db/init.sql` 没有后台账号种子**！`t_admin` 只由启动时的 `DataInitializer` 创建（仅当表为空）
   → **复位数据后必须 `docker-compose up -d --force-recreate api`**，只导数据不重建 → 后台登录必然失败。
5. **MySQL 中文「双重编码」坑**：`docker exec` 进 MySQL 时不加 `--default-character-set=utf8mb4`，
   客户端会按 latin1 解释 UTF-8 字节再存入 utf8mb4，**存储真的坏掉**（非显示问题）：
   `HEX(name)` 变成 `C3A5C2BC...`，页面显示 `å¼ å†²`，且**该人登录必然报 1005 姓名不符**。
   **最稳做法：用 `UNHEX('E5BCA0E586B2')` 直接写字节**，不依赖连接字符集。
6. **小程序 `app.json` 不要开 `"lazyCodeLoading": "requiredComponents"`**——
   开发者工具编译不受影响、**只在真机生效**，会报
   `Component is not found in path "wx://not-found"` 导致**真机白屏**（本项目已移除，回归里已钉死）。
7. **真机白屏的三条已加防线，勿删**（`test-miniprogram-native.py` 已断言）：
   - `app.js` 的 `wx.cloud.init` 必须 try/catch（`onLaunch` 抛异常 → 整站白屏且真机零提示）；
   - `utils/api.js` 的 `wx.cloud.callFunction` 必须 try/catch 转 `Promise.reject`
     （基础库过低时它会**同步抛错**，`.catch` 接不住，直接冒泡到页面 → 整页渲染不出来）；
   - 页面首屏必须有 `loading` + `errMsg` + 重试，且**不丢原始 errMsg**。
8. **服务器上传文件默认 644**——设成 700 会让容器内非 root 用户读不到挂载文件。
9. 改线上数据建议用 `ssh_deploy.py --script` 传一个
   「从 `deploy/server/.env` 读密码 + `docker exec` 跑 SQL」的脚本（避开嵌套引号地狱），
   中文一律 `UNHEX`，改完回读验证。

---

## 九、未完成事项 / 待决策

| # | 事项 | 说明 |
| --- | --- | --- |
| 1 | **主体与名称决策（最高优先级）** | 当前是**个人主体**，而「华中商贸配送中心」含企业字号，**名称审核大概率过不去**；个人主体也没有电商/百货/食品/配送类目。换名称通常＝换主体＝换 AppID＝**云开发环境需重建**。**建议提交审核前先定**：换个中性名称（如"教职工慰问品领取"），还是转成学校/单位的企业主体（企业主体还能解开 90 人体验成员上限） |
| 2 | 小程序备案 | 境内强制，未备案不能上架。云函数通道下**不需要填域名与服务器接入信息**，建议尽早启动（管局审核 1–20 个工作日） |
| 3 | 提交审核并发布 | 体验版 `1.0.0` 已上传，待选服务类目提交（个人主体建议「工具 → 信息查询」或「工具 → 预约/报名」，描述里避免出现"在线销售 / 商城 / 配送"） |
| 4 | 体验成员 | 个人主体上限 15 人，只够管理员与项目成员用；对外需靠正式发布 |

---

## 十、建议的上手顺序

1. 读 `.workbuddy/memory/MEMORY.md`（项目事实与结论的浓缩版）；
2. 读 `docs/华中商贸配送中心-开发文档-v4.0.md`（最完整）；
3. 按第三节把本地环境跑起来，登录三个测试账号走一遍主流程；
4. 跑一次 `python .workbuddy/tools/run-regression.py`，确认 428 项全绿；
5. 拿到第五节凭据后，验证线上入口与后台；
6. 推动第九节第 1、2 项决策。
