# 华中商贸配送中心 · 上手指南

> 用途：接手人从零到能动手干活的最短路径。
> 配套资料按阅读顺序：本指南 → `.workbuddy/memory/MEMORY.md`（项目结论浓缩版）→ `docs/华中商贸配送中心-开发文档-v4.0.md`（最完整）。
> 所有技术细节、坑、线上配置的原始出处是 `交接说明.md`，二选一即可，本指南是它的「开始前先读版」。

---

## 1. 这个项目是什么

**教职工慰问品线上选购与配送信息采集平台**，供货与配送的履约方是华中商贸配送中心。
它是"选品 → 提交配送信息"的单向采集系统，**不是**通用物流/聚合配送平台（v1.0/v2.0 方向已废弃，勿照着开发）。

### 业务约束（不可违背，改动务必守住）

| 约束 | 落地方式 |
| --- | --- |
| 收到配送信息起 **10 日内发货** | SLA 倒计时引擎，T+7/8/9/10 四道预警，超时登记原因 |
| **上楼入户、送达门牌** | 地址强制校验到门牌；配送单固定带「上楼入户」标记 |
| 未经允许不得放代收点 | C 端授权开关**默认关闭**，结果写入配送单同步承运商 |
| 不允许私自更改方案内容 | 套餐 C 端只读；后台变更强制填原因留痕；订单存套餐快照 |
| 全国包邮 / 货品充足 | 全站标识 + 偏远地区公示时效；库存管理与超卖拦截 |
| **导出个人信息必须留痕** | `t_operation_log` 记录操作人/角色/IP/UA/模块/动作/筛选/条数；审计写失败不得影响导出主流程 |
| 名单是唯一身份来源 | `t_grantee` 只有姓名/手机号必填，二要素核验登录 |

---

## 2. 上手路线（先做什么、后做什么）

1. 通读本指南，形成整体印象；
2. 读 `.workbuddy/memory/MEMORY.md`（项目事实与踩坑结论的浓缩，快速建立信任）；
3. 按第 4 节把**本地环境跑起来**，用三个测试账号把主流程走一遍（登录 → 领套餐 → 提交配送信息 → 看订单轨迹）；
4. 跑一次回归（第 5 节），确认 428 项断言全绿，证明环境完整；
5. 面向上线：理解线上现状（第 8 节），处理**已写好但未部署的加固**（第 9 节）；
6. 拉通决策：推动第 10 节的「主体与名称决策」等关键事项。

> ⚠️ **第 5 步需要凭据**：根目录 `.env`（真实密钥）、服务器 SSH、短信平台密钥等**都不在交接包内**，
> 按 `交接说明.md` 第五节由交接方通过安全渠道单独提供。没拿到前先做本地部分。

---

## 3. 环境准备（前置条件）

| 依赖 | 版本要求 | 用途 |
| --- | --- | --- |
| Docker Desktop | 含 Compose | 起 MySQL / Redis / API / Nginx 全套 |
| JDK | 17 | 后端（容器内构建，本机主要用于看代码/排错） |
| Node | 18+ | 小程序端、云函数、部分脚本 |
| Python | 3.10+ | 回归与部署工具（`.workbuddy/tools/*.py`） |
| 微信开发者工具 | 最新稳定 | 小程序原生端联调/真机 |

> 本机不需要装 Maven / MySQL / Redis——后端构建和运行都在 Docker 里。

---

## 4. 本地启动（约 10 分钟）

```bash
# 1. 准备环境变量（.env 不要提交到版本库）
cp .env.example .env

# 2. 至少改这两个（其余保持默认即可跑通）
#    MYSQL_ROOT_PASSWORD   → 换成你自己的
#    ADMIN_INIT_PASSWORD   → 后台初始密码，登录后立刻改

# 3. 启动全套（首次构建需几分钟）
docker-compose up -d --build

# 4. 等 API healthy
docker-compose ps

# 5. 自检
curl http://localhost:8080/healthz          # 期望 {"status":"UP", ...}
curl http://localhost/api/client/auth/mode  # 期望 mode:sms
```

访问入口：

| 入口 | 地址 |
| --- | --- |
| 消费者端 H5 | http://localhost/ |
| 运营后台 | http://localhost/admin/ |
| 接口（调试直达后端） | http://localhost/api/ 或 http://localhost:8080/ |

后台账号：`admin` / `operator` / `viewer`，初始密码＝`.env` 里的 `ADMIN_INIT_PASSWORD`。
后台账号由 **API 启动时的 `DataInitializer` 创建，且仅在 `t_admin` 表为空时执行**。

### 本地登录 C 端测试账号

`.env.example` 默认 `LOGIN_MODE=sms` + `SMS_PROVIDER=mock`（演示通道，不真发短信）；
**本地拿着任意演示手机号，验证码固定填 `123456` 即可登录**（`MOCK_SMS_CODE`）。

| 手机号 | 姓名 | 状态 |
| --- | --- | --- |
| `13900139003` | 刘嘉怡 | 未领取（可走完整领取流程） |
| `13800138000` | 张明远 | 已领取（可看订单与物流轨迹） |
| `15369865664` | 张冲 | 未领取（工号 10000，河北大学·后勤处） |

---

## 5. 验证环境是否完整：跑一次性回归

```bash
python .workbuddy/tools/run-regression.py        # 一键全跑
python .workbuddy/tools/run-regression.py carrier  # 只跑匹配关键字的套件
```

- 它帮你做：复位数据 → 清 Redis → **按 .env 重建 api 容器**并等 healthy → 跑 10 个套件 → 汇总。
- 当前基线：**428 项断言全绿**。
- ⚠️ 目录纪律：**`.workbuddy/tools/` 必须保持现有层级**——脚本用 `Path(__file__).parents[2]` 定位项目根，挪走就全部失效。

另三个独立套件（不必每次跑）：

| 脚本 | 项数 | 作用 |
| --- | --- | --- |
| `test-miniprogram-native.py` | 28 | 小程序静态校验（防真机白屏） |
| `test-cloudfunction-contract.py` | 36 | 云函数契约（加载真实 `index.js` 打真实后端） |
| `verify-cloudfunction.js` | 9 | 小程序模拟器端到端（需开发者工具开自动化） |

> `test-sms-live.py` 需设 `HZ_SMS_PHONE` 才会跑，**它会真发一条短信**。

---

## 6. 项目结构速览

| 目录/文件 | 内容 |
| --- | --- |
| `server/` | Java 后端（Spring Boot 3.2 + MyBatis-Plus），含 `Dockerfile`/`pom.xml` |
| `admin/` | 运营后台（单文件 `index.html`，Vue 3 + Element Plus + ECharts） |
| `miniapp/` | 消费者端 H5（单文件 `index.html`） |
| `miniprogram/` | 微信小程序端（**10 个原生页面**，非 web-view 壳） |
| `cloudfunctions/api/` | 云函数转发入口（零第三方依赖，只用 Node 内置模块） |
| `db/init.sql` | 建表 + 种子数据（测试名单、套餐、公告） |
| `deploy/` | Nginx 配置 + 服务器编排 `deploy/server/docker-compose.yml` + 线上 `.env` 模板 |
| `docs/` | 开发文档 v4.0（现行）/ v3.0 / v2.0 / archive |
| `docker-compose.yml` | 本地开发编排（MySQL + Redis + API + Nginx） |
| `.env.example` | 环境变量模板（已脱敏） |
| `project.config.json` | 小程序工程配置（AppID、miniprogramRoot、cloudfunctionRoot） |
| `.workbuddy/` | 回归/部署工具 + 项目知识记录（`memory/`） |

---

## 7. 关键纪律与坑（动手必读）

1. **改 `.env` 后必须 `docker-compose up -d --force-recreate api`**——`docker restart` 不重读 `.env`；
   同理，**复位数据/导数据后也必须重建 api 容器**，否则后台登录必然失败（`t_admin` 为空、`DataInitializer` 不执行）。
2. Windows 上 **`docker compose`（CLI 插件）可能突然不可用**（`unknown command: docker compose`），
   改用独立二进制 **`docker-compose`**；`run-regression.py` 已自动探测二者。
3. 数据库中文**双重编码坑**：手动 `docker exec` 进 MySQL 时**必须带 `--default-character-set=utf8mb4`**，
   或干脆用 `UNHEX('E5BCA0E586B2')` 直接写字节，否则中文存成 `å¼ å†²`，且该人登录必报 1005 姓名不符。
4. 小程序 `app.json` **不要开 `"lazyCodeLoading": "requiredComponents"`**——只在真机生效，会白屏。
5. 真机白屏三条防线勿删：`wx.cloud.init` 要 try/catch；`wx.cloud.callFunction` 要 try/catch 转 `Promise.reject`；
   页面首屏必须有 `loading` + `errMsg` + 重试，且不丢原始 errMsg。
6. **交接怪别删**：`admin.bak.html` 等 `*.bak.*`、`docs/archive/` 是历史备份/追溯，不要照着开发。
7. 交付前必跑回归（第 5 节）；小程序改动后跑 `test-miniprogram-native.py`。

---

## 8. 线上环境现状（接手即知）

| 项 | 当前值 |
| --- | --- |
| C 端登录 | **`LOGIN_MODE=sms`**（手机号 + 短信验证码；姓名核验通道已关，再传姓名返回 1003） |
| 短信通道 | `SMS_PROVIDER=hbu`（河北大学统一短信平台，**真实下发**；`SMS_HBU_KEY` 已补齐） |
| 对外入口 | http://221.192.236.175/ （H5）· /admin/（后台）· /healthz（健康检查） |
| 服务器 | 内网 `10.191.19.25`（Ubuntu 20.04 / 4C8G），部署目录 `/opt/hz-delivery`，只对外暴露 80 |
| **进服务器方式** | **SSH 仅校园网内可达**，**22 端口未映射公网**——校外必须**先连学校 VPN**，否则连不上 |
| 小程序 | AppID `wx67f6685a1a7bc5f3`，云环境 `cloud1-d2gar70gv5b552827`，体验版 1.0.0 已上传，**未备案、未提交审核** |

**⚠️ 必须知道的两件事**：

- **线上名单全是演示假号码**，短信是真实通道 → 一旦有人触发发送，**会真发短信到陌生人手机**（费用+骚扰）。
  **正式使用前必须整表换成甲方提供的真实手机号**；目前只有张冲 `15369865664`（真实号码）能收到验证码登录。
  因此换名单前**不宜公开对外宣传入口**。
- **线上后台 admin 密码 ≠ 开发机 `.env` 的 `ADMIN_INIT_PASSWORD`**，以服务器 `/root/hz-initial-credentials.txt` 为准；
  线上后台 UI **没有「编辑教职工」功能**（只有详情/重置额度/覆盖式导入），改线上名单要么 SSH 改库，
  要么走后台接口 `POST /api/admin/grantees`（body 带 `id` 即走更新分支）。

### 部署方式（服务器不需要 JDK/Maven/外网依赖）

```
开发机：docker compose build api → docker save | gzip
   ↓ SFTP（可用 .workbuddy/tools/ssh_deploy.py）
服务器：docker load → docker compose up -d
```

现成脚本 `ssh_deploy.py`：凭据只从环境变量读、不落盘（`HZ_SERVER_HOST/PORT/USER/PASS`），
支持 `--cmd / --script / --put / --get / --check`。服务器 Docker Hub 直连不通，需配 Daocloud 镜像源。

---

## 9. 接手后最该推进的事（按优先级）

| # | 事项 | 说明 / 怎么动手 |
| --- | --- | --- |
| 1 | **主体与名称决策（最高优先级）** | 现在是**个人主体**，名称「华中商贸配送中心」含企业字号，**小程序名称审核大概率过不去**；个人主体也无电商/食品/配送类目。换名称≈换主体≈换 AppID→云环境重建。提交审核前必须先定。 |
| 2 | **小程序备案** | 境内强制，未备案不能上架。云函数通道下**不需填域名与服务器接入信息**；管局审核 1–20 个工作日，**尽早启动**。 |
| 3 | **提交审核并发布** | 体验版 1.0.0 已上传，选服务类目提交（个人主体建议「工具→信息查询」或「工具→预约/报名」，名称/描述避免"在线销售/商城/配送"）。 |
| 4 | **体验成员上限** | 个人主体仅 **15 人**，够管理员/项目成员用；全体教职工必须靠正式发布。 |
| 5 | **两项加固尚未上线** | 源码已改好但未部署（本机 Docker 配过不可用手动代理导致构建失败）：① 验证码每日上限 `SMS_DAILY_LIMIT`；② 名单探测接口收敛（`/api/client/auth/check` 不再回脱敏姓名/单位/部门）。需在**任一能正常构建的机器**上 `docker compose build api` → save/load → `--force-recreate api`。 |

### 上手后的第一份工作清单

1. 本地全流程跑通（三账号走主流程 + 回归全绿）——证明环境没问题；
2. 拿到凭据（第 2 节提醒），登线上后台确认入口可用；
3. 把第 9 节第 5 项「加固部署」落地（含验证码频控 + 名单接口收敛）；
4. 向甲方要**真实教职工名单**，设计替换流程（注意 `UNHEX` 写中文 + 改完回读验证）；
5. 与甲方对齐第 1 项「主体与名称」决策，之后才好走备案 → 审核 → 发布。

---

## 10. 常用命令速查

```bash
# 本地起/停/看日志
docker-compose up -d --build
docker-compose ps
docker-compose logs -f api
docker-compose down                    # 全停

# 改完 .env 或复位数据后，重建 api（记住：不重建不生效）
docker-compose up -d --force-recreate api

# 回归
python .workbuddy/tools/run-regression.py

# 线上健康检查
curl http://221.192.236.175/healthz
curl http://221.192.236.175/api/client/auth/mode

# SSH 工具（先 export 四个环境变量凭据）
python .workbuddy/tools/ssh_deploy.py --check
```