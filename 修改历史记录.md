# 修改历史记录

> 记录本次维护会话(2026-09-17)在接入者本机(lenovo, Windows 11)搭建与跑通本地环境的全部改动,
> 供接手人追溯「改了什么、为什么改、怎么重现」。
> 配套参考:`上手指南.md`(上手路线)、`交接说明.md`(线上现状与移交原则)。

---

## 一、本次改动总览

| # | 文件 / 位置 | 改动 | 原因 |
| --- | --- | --- | --- |
| 1 | 本机 `C:\Users\lenovo\.docker\daemon.json`(项目外) | 新增 `registry-mirrors: ["https://docker.m.daocloud.io"]` | 本机 Docker Hub 直连不稳,后端镜像 `maven/temurin` 构建时拉取超时;daocloud 镜像源实测可达 |
| 2 | `.env` | `MYSQL_PORT=3306 → 3307` | 本机有原生 `mysqld.exe`(Windows 服务)占用宿主 3306,容器无法绑定端口 |
| 3 | `.workbuddy/tools/api-smoke.py` | 4 处名单统计断言更新值的基数 36→37 等 | `db/init.sql` 于前次会话追加了张冲(未领取),名单由 36 人变为 **37 人**,原断言过期 |
| 4 | `.workbuddy/tmp/walk-golden-path.py` | 新增(演练脚本,临时) | 用于一键复现 C 端主流程(未领取/已领取两角色的黄金路径) |
| 5 | 无文件改动 | 运行/回归需带环境变量(见第三节) | 中文目录名 + Windows GBK 控制台两个隐性环境问题 |

> 交付代码仅改动 **3** 处(`api-smoke.py` 断言数值);其余均为本机环境配置或临时脚本。

---

## 二、逐项说明

### 1. Docker 镜像源(仅本机,项目外)

- **现象**:`docker-compose up -d --build` 在 `docker build` 阶段报
  `failed to fetch oauth token / dial tcp ...auth.docker.io... connectex`。
  mysql/redis/nginx 基础镜像能拉,maven/eclipse-temurin 构建镜像拉不下来。
- **处置**:在 Docker Desktop 引擎配置 `C:\Users\lenovo\.docker\daemon.json` 增加
  `"registry-mirrors": ["https://docker.m.daocloud.io"]`,重启 Docker Desktop 后生效
  (`docker info --format '{{json .RegistryConfig}}'` 可见该镜像源)。
- **验证**:`hz-delivery-server:1.0.0` 构建成功。
- **不属于项目代码**,换机器无需此配置,但本机重装/换 Docker 后需重新配置。

### 2. `.env:MYSQL_PORT`

- **现象**:容器启动报 `bind: Only one usage of each socket address`(109)。
  查 `netstat` 确认宿主 3306 被 PID 9880(`mysqld.exe`,Windows 原生 MySQL 服务)占用。
- **处置**:`.env` 中 `MYSQL_PORT` 改为 `3307`。容器**内部仍是 3306**,仅宿主映射端口变化;
  回归与工具均通过 `docker exec hz-mysql` 在容器内连接,不受影响。
- **注意**:若本机 3306 释放,可改回 3306;保持与现有工具一致的关键是**不改容器内部端口**。

### 3. `.workbuddy/tools/api-smoke.py`(本次唯一交付代码改动)

9/17 前次会话向 `db/init.sql` 名单追加了**张冲**(工号 10000 / 手机号 15369865664 / 未领取),
名单总量 36 → 37。`api-smoke.py` 未同步,导致回归 smoke 套件 4 项失败(实际值比断言多 1)。
已按数据库实况核对后更新断言:

| 断言 | 原值 | 新值 | 依据 |
| --- | --- | --- | --- |
| 名单总数正确 | 36 | **37** | `SELECT COUNT(*) FROM t_grantee` = 37 |
| 分页总数正确 | 36 | **37** | 同上 |
| 未领取筛选正确 | 24 | **25** | 张冲未领取,未领取 24+1 |
| 按单位筛选(河北大学)正确 | 36 | **37** | 全表均为河北大学 |

「已领取人数==12」「订单总数==12」等断言未受影响(张冲未领取、不新增订单)。

### 4. `.workbuddy/tmp/walk-golden-path.py`(临时演练脚本)

独立的小脚本,走 C 端主流程:未领取账号完整领取(登录→套餐→提交配送信息→订单出现在列表/详情),
已领取账号看历史订单与本人运单轨迹、重复领取被拦截、不存在运单返回 1301。
属 `.workbuddy/tmp/` 临时区,不参与交付打包(与交接包打包脚本的排除规则一致),可随时删除。

---

## 三、本机运行/回归的环境要求(隐性)

以下两个是目录名与 Windows 编码导致的隐性要求,**文档已存忘(长期记忆),留给后续会话**:

1. **`COMPOSE_PROJECT_NAME=hz-delivery`**:项目目录「华中商贸配送中心-项目交接包」为纯中文,
   `docker-compose` 推导项目名时为空,直接报 `project name must not be empty`。
   所有 compose 命令(含 `run-regression.py` 内部的重建步骤)都需先 `export COMPOSE_PROJECT_NAME=hz-delivery`。
2. **`PYTHONIOENCODING=utf-8`**:Windows GBK 控制台打印子进程输出会崩
   `UnicodeEncodeError`。跑回归/脚本统一用
   `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python ...`。

---

## 四、验证结果

| 项目 | 结果 |
| --- | --- |
| 容器 | mysql / redis / api / nginx 四容器全部 up,api **healthy** |
| 后端健康 | `/actuator/health` → `{"status":"UP"}`(注:`/healthz` 为 nginx 静态返回,后端真健康检查是 `/actuator/health`) |
| C 端登录模式 | `/api/client/auth/mode` → `mode:sms` + `mockCode:123456`(本地 mock 通道,验证码固定 123456) |
| 全量回归 | **418 通过 / 0 失败**(smslive 因未设 `HZ_SMS_PHONE` 按设计跳过;cloudfn 无 `HZ_CF_TOKEN` 时登录相关用例按设计跳为 18 项) |
| 主流程演练 | 刘嘉怡(未领取)完整领取成功→订单 #13;张明远(已领取)本人运单轨迹 5 条可查、重复领取 1103、不存在运单 1301 |
| 后台 | admin 登录成功 |

---

## 五、备注

- 本次结论与隐性坑已同步进长期记忆(`local-dev-env-lenovo`),后续会话自动衔接,无需重新排查。
- 改 `.env` 或复位数据后,**必须 `--force-recreate api`**(`docker restart` 不重读 `.env`;
  且 `init.sql` 无后台账号种子,依赖应用启动时 `DataInitializer` 重建 `t_admin`)。
- 本机 80 端口空闲、6379 归本项目 redis 使用;若与其它服务冲突,`WEB_PORT/REDIS_PORT` 可调。